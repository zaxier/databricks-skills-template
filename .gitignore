__pycache__/
*.pyc
.DS_Store
.venv/
.env

# Local sync state — per-user, per-machine; never committed.
.skills-sync-state.json

# User-specific workspace targets (copy from workspaces.example.toml).
config/workspaces.toml
